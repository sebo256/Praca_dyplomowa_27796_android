<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Praca_dyplomowa_27796_android"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".views.CalendarJobListView"
            android:exported="false" />
        <activity
            android:name=".views.JobApplyToActivityView"
            android:exported="false" />
        <activity
            android:name=".views.AddJobActivity"
            android:exported="false" />
        <activity
            android:name=".views.MainActivityView"
            android:exported="false" />
        <activity
            android:name=".views.RegisterActivityView"
            android:exported="false" />
        <activity
            android:name=".views.LoginActivityView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>